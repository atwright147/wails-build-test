name: Wails build

on: [push, pull_request]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        build: [
          {platform: linux/amd64, os: ubuntu-latest, artifact: linux-amd64},
          {platform: windows/amd64, os: windows-latest, artifact: windows-amd64},
          {platform: darwin/amd64, os: macos-latest, artifact: macos-amd64},
          {platform: darwin/arm64, os: macos-latest, artifact: macos-arm64}
        ]
    runs-on: ${{ matrix.build.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dAppServer/wails-build-action@main
        with:
          build-name: build-test-${{ matrix.build.platform }} # Unique name!
          build-platform: ${{ matrix.build.platform }}
          build-obfuscate: true
      - name: Package macOS application
        if: matrix.build.platform == 'darwin/amd64' || matrix.build.platform == 'darwin/arm64'
        run: |
          ls -la build/bin/
          chmod +x build/bin/*.app/Contents/MacOS/*
          ditto -c -k --keepParent "build/bin/build-test-${{ matrix.build.platform }}.app" "build/bin/${{ matrix.build.artifact }}.zip" # Correct app name
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-test-${{ matrix.build.platform }}-${{ matrix.build.artifact }} # Unique name!
          path: build/bin/build-test-${{ matrix.build.platform }}* # Correct path
